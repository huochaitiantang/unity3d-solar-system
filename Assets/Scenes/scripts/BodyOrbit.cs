using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BodyOrbit : MonoBehaviour
{
    public GameObject centralBody;
    public double R;        // average orbit radius, m
    public double V;        // average orbit linear velocity, m/s
    public double angle;    // orbit inclination, degreee, orbit of earth = 0

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
